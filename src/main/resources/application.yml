spring:
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:mysql://localhost:3306/github-developer-skills
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update

commands:
  fetch:
    token: XXX

logging:
  level:
    root: ERROR
    org.springframework: ERROR
